import React from 'react';

export default function Card(props) {
	// basic component skeleton — minimal JSX and props usage
	return (
		<div className="card">
			{/* Card content goes here */}
		</div>
	);
}